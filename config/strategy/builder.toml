# Строитель стратегия (Builder)
# Фокусируется на строительстве мастерских и покупке земель.
# Высоко ценит дерево (основной строительный материал) и деньги (оплата строительства).
# Большой горизонт планирования — мастерские окупаются в будущем.
# Низкая жадность — ресурсы нужны для строительства, а не для накопления.

needs_weight = 4.0
vp_weight = 2.0
future_weight = 3.0

# Exponents: дерево и деньги — критические ресурсы для строительства
wood_exponent = 3.5
food_exponent = 2.5
metal_exponent = 2.0
weapon_exponent = 1.0
wax_exponent = 1.0
wool_exponent = 1.0
money_exponent = 3.0
people_exponent = 2.5
reputation_exponent = 2.5
action_cards_exponent = 1.5
law_cards_exponent = 1.5
war_cards_exponent = 0.5

# Weights: дерево и деньги — приоритет, люди/репутация тоже важны (масштаб)
wood_weight = 6.0
food_weight = 4.0
metal_weight = 3.0
weapon_weight = 1.5
wax_weight = 1.5
wool_weight = 2.0
money_weight = 5.0
people_weight = 4.0
reputation_weight = 4.0
action_cards_weight = 2.0
law_cards_weight = 2.0
war_cards_weight = 0.5

greed_exponent = 0.5       # 0.0 - 2.0 (низкая жадность — тратит на строительство)
discount_exponent = 0.7    # 0.0 - 0.99 (длинный горизонт — мастерские окупаются позже)
